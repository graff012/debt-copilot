packages:
  - "apps/*"
  - "packages/*"

# Non-interactive `pnpm approve-builds`: Tailwind v4's resolver needs its
# postinstall script. Nothing else is allowed to run install scripts.
onlyBuiltDependencies:
  - unrs-resolver
